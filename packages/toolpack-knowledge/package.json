{
  "name": "toolpack-knowledge",
  "version": "1.0.0",
  "description": "Knowledge capabilities for the Toolpack SDK",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "watch": "tsc -w",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
