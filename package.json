{
  "name": "toolpack-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/toolpack-knowledge && npm run build -w packages/toolpack-sdk",
    "test": "npm run test -w packages/toolpack-knowledge && npm run test -w packages/toolpack-sdk",
    "lint": "eslint packages/*/src/**",
    "version": "node scripts/update-version.js"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
